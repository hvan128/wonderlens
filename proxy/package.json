{
  "name": "wonderlens-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel serverless proxy giấu OpenAI key cho WonderLens (recognize + generate).",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.0",
    "typescript": "^5.6.0"
  }
}
