{
  "name": "wonderlens-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vercel serverless proxy giấu OpenAI key cho WonderLens (recognize + generate).",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod",
    "pregen:videos": "node scripts/pregen-hero-videos.mjs"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.0",
    "typescript": "^5.6.0"
  }
}
